# Backend URL to ping
BACKEND_URL=https://anac-backend.onrender.com

# Endpoint to ping (should be a lightweight health check endpoint)
PING_ENDPOINT=/api/health

# Cron schedule (default: every 5 minutes)
# Format: minute hour day-of-month month day-of-week
CRON_SCHEDULE=*/5 * * * *

# Port for the health check server
PORT=3001
