# Backend URLs to ping (comma-separated for multiple servers)
BACKEND_URLS=https://anac-backend.onrender.com,https://ipnet-attendance.onrender.com/api

# Endpoint to ping (should be a lightweight health check endpoint)
PING_ENDPOINT=/api/health

# Cron schedule (default: every 1 minute)
# Format: minute hour day-of-month month day-of-week
CRON_SCHEDULE=*/1 * * * *

# Port for the health check server
PORT=3001
